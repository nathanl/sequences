require 'spec_helper'

describe Sequences do
  it 'has a version number' do
    expect(Sequences::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
